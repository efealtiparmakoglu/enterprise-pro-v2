apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: enterprise-pro-v2-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: enterprise-pro-v2.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: enterprise-pro-v2-service
            port:
              number: 80
